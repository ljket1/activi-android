<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.monash.ljket1.activi">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.location.gps" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activites.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activites.LoginActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activites.CreateEventActivity"
            android:label="Create Event"
            android:parentActivityName=".activites.MainActivity" />
        <activity
            android:name=".activites.SetLocationActivity"
            android:label="@string/title_activity_set_location" />
        <activity android:name=".activites.ViewEventActivity" />
        <activity android:name=".activites.ViewProfileActivity" />
        <activity
            android:name=".activites.BarcodeActivity"
            android:label="Barcode"
            android:parentActivityName=".activites.MainActivity" />
        <activity
            android:name=".activites.MyEventsActivity"
            android:label="My Events"
            android:parentActivityName=".activites.MainActivity" />
        <activity android:name=".activites.ScanActivity" />
        <activity android:name=".activites.NotificationsActivity" />
        <activity android:name=".activites.RateActivity" />
        <activity android:name=".activites.CreateProfileActivity"></activity>
    </application>

</manifest>